[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "zygote-harness"
version = "0.1.0"
description = "Python client for the Zygote JSON-RPC 2.0 Harness"
authors = [
    {name = "Mindfire", email = "oss@mindfire.com"}
]
license = {text = "Apache-2.0"}
requires-python = ">=3.7"
dependencies = []

[project.urls]
Homepage = "https://github.com/mindfire/zygote"
